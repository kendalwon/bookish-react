import React from 'react';

const IndicatorButton = props => {
    return (
      <button className={this.state.toy == {props.index} ? "indicator indicator-active" : "indicator"
        }
      onClick="goToSlide(0)"></button>
  )
}

export default IndicatorButton;